name: view_model_binder
description: A convenient and easy to use, stream-based Flutter architecture
version: 0.1.0
homepage: https://github.com/zaiste/ViewModelBinder

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
